<div class="h-screen flex">
  <Router routes="routes" guards="guards" t-slot-scope="router">
    <div t-if="router.currentRoute and router.currentRoute.route.name !== 'Login'" class="h-screen w-20 bg-compassion relative shadow-2xl z-50 flex flex-col items-center">
      <img src="/logo_simple.png" class="pb-3 pt-4 px-3 bg-white" />
      <RouterLink to="'/'" t-slot-scope="scope">
        <MenuButton tooltip="'Home'" active="scope.active">
          <Icon prefix="'fas'" icon="'home'" class="'text-xl'" />
        </MenuButton>
      </RouterLink>
      <RouterLink to="'/users'" t-slot-scope="scope">
        <MenuButton tooltip="'Users'" active="scope.active">
          <Icon prefix="'fas'" icon="'user-group'" class="'text-xl'" />
        </MenuButton>
      </RouterLink>
      <RouterLink to="'/letters'" t-slot-scope="scope">
        <MenuButton tooltip="'Letters'" active="scope.active">
          <Icon prefix="'fas'" icon="'envelope'" class="'text-xl'" />
        </MenuButton>
      </RouterLink>
      <RouterLink to="'/text-edit/1'" t-slot-scope="scope">
        <MenuButton tooltip="'Texts'" active="scope.active">
          <Icon prefix="'fas'" icon="'comments'" class="'text-xl'" />
        </MenuButton>
      </RouterLink>
      <div class="mt-auto mb-4">
        <MenuButton tooltip="'Settings'" class="'cursor-pointer'">
          <Icon prefix="'fas'" icon="'gear'" class="'text-xl'" />
        </MenuButton>
      </div>
    </div>
    <div class="flex-1">
      <RouterView route="router.currentRoute" />
    </div>
  </Router>
</div>